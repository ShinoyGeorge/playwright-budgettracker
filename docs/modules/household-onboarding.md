# Household Onboarding & Auth — Test Notes

## Coverage

- New household registration, reviewed and approved via the `/admin` UI.
- Confirmation shown on valid new-household registration (`TRACKER-TC-8`).
- New-household registration request persisted as pending and visible to
  an admin (`TRACKER-TC-9`).
- New-household registration rejected when the email is already in use
  (`TRACKER-TC-11`).
